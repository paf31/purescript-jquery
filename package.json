{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "test": "pulp build -I test && pulp browserify -m Test.Main --to html/index.js",
    "build": "pulp build"
  },
  "devDependencies": {
    "pulp": "^14.0.0",
    "purescript-psa": "^0.7.3",
    "rimraf": "^3.0.2"
  }
}
