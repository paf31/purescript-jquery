{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "test": "pulp browserify -m Test.Main --to html/index.js",
    "build": "pulp build"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript-psa": "^0.7.2",
    "purescript": "^0.12.0",
    "rimraf": "^2.6.2"
  }
}
